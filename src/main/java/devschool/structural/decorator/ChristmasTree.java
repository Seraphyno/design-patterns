package devschool.structural.decorator;

/**
 * We want to create a tree for Christmas, but depending on the owner it should be
 * decorated differently
 */
public interface ChristmasTree {

    String decorate();
}
